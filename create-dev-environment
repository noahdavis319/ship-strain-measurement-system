
# Runs commands to quickly create the developer environment


# Creates virtual environment
python3 -m venv venv

# Activates the virtual environment
source venv/bin/activate

# Installs dependencies
pip install pybuilder

# Create the PyCharm IDE integration
pyb pycharm_generate

# Run the default pybuilder tasks
pyb -v install
